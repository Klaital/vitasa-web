<p id="notice"><%= notice %></p>

<h1>Shift #{@shift.calendar.date}/#{@shift.start_time}</h1>

<p>
  <strong>Timing</strong>
  <%= @shift.start_time %> - <%= @shift.end_time %> (<%= Tod::Shift.new(@shift.start_time, @shift.end_time).duration / 3600.0 %> hours)
</p>

<h2>Staffing</h2>

<p>
  <strong>Basic Certification:</strong>
  <%= @shift.efilers_needed_basic %> needed, <%= @shift.efilers_signed_up('Basic') %> signed up.
</p>

<p>
  <strong>Advanced Certification:</strong>
  <%= @shift.efilers_needed_advanced %>
</p>

<%= link_to 'Edit', edit_site_calendar_shift_path(@shift.calendar.site.slug, @shift.calendar, @shift) %> |
<%= link_to 'Back', site_calendar_shifts_path(@shift.calendar.site.slug, @shift.calendar) %>
